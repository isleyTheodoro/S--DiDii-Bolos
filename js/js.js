const myModalEl = document.querySelector('#modal-body')

const modal = new bootstrap.Modal(modal-bodyEl) // initialized with defaults

const configObject = { keyboard: false }
const modal1 = new bootstrap.Modal(modal-bodyEl, configObject) // initialized with no keyboard